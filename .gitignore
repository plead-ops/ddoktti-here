# dependencies
node_modules/
.pnpm-store/

# build output
dist/
out/
*.tsbuildinfo

# tauri / rust
apps/desktop/src-tauri/target/
apps/desktop/src-tauri/gen/

# env / secrets
.env
.env.*
!.env.example

# signing keys (NEVER commit) — MSIX 코드서명 개인키/PFX 포함
# 공개 인증서(identity/ddoktti-cert.cer)만 커밋, 개인키(*.pfx/*.pem/*.key)는 절대 금지
*.key
*.pfx
*.pem

# logs
*.log
pnpm-debug.log*

# os
.DS_Store
Thumbs.db
